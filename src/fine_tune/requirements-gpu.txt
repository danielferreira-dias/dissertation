# GPU stack pinned for the fine-tune campaign — Unsloth-based.
#
# Unsloth bundles its own pinned versions of torch, peft, trl, transformers,
# accelerate, bitsandbytes, flash-attn, flash-linear-attention, and triton —
# matched per (cuda, torch) extra. We pick the extra that fits the pod's
# NVIDIA driver R565 (CUDA 12.7 reported, supports toolkit ≤ 12.6):
#
#   cu124-torch260   torch 2.6.0 + cu124, current pod baseline
#
# If the pod ships a newer driver (R570+, CUDA 12.8+), bump to:
#   cu128-torch280   torch 2.8.0 + cu128
#
# Reference: https://docs.unsloth.ai/get-started/install/pip-install
#
# DO NOT replace `unsloth[cu124-torch260]` with bare `unsloth`. Bare-install
# auto-resolution can pull a torch version that exceeds the pod's CUDA cap
# (this killed an earlier run when fla 0.5.0 forced torch 2.7+ → cu130).

unsloth[cu124-torch260]

# Pure-Python deps that are NOT bundled in Unsloth's extras.
pyyaml>=6.0
tensorboard>=2.15
sentencepiece>=0.2
pillow>=10.0
numpy>=1.26,<3.0
pandas>=2.2

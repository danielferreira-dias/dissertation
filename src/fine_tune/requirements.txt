# Fine-tuning requirements for both backends.
# Install on RunPod L40S 48GB before running training scripts.
#
# Usage:
#   pip install -r src/fine_tune/requirements.txt

# ── PyTorch (CUDA 12.1) ────────────────────────────────────────
torch>=2.4.0
torchvision

# ── Core HF stack (used by both backends) ──────────────────────
transformers>=4.46.0
accelerate>=0.34.0
datasets>=3.0.0
peft>=0.13.0
bitsandbytes>=0.44.0
trl>=0.11.0
sentencepiece
protobuf

# ── Vision / IO ────────────────────────────────────────────────
pillow
einops

# ── Unsloth backend (optional but recommended) ─────────────────
# Unsloth pins specific versions of torch/transformers — install separately
# if you hit conflicts:
#   pip install unsloth "xformers<0.0.27"
unsloth

# ── Utilities ──────────────────────────────────────────────────
tqdm
